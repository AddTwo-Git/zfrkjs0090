sap.ui.define(["sap/ui/base/Object"], function (Object, JSONModel) {
  "use strict";

  const nameSpace = "readians.zfrkjs0090";
  const moduleName = nameSpace + ".model.Validator";

  const validator = Object.extend(moduleName, {
    constructor: function (i18n) {
      this.i18n = i18n;
    },

    // Add Validate functions
  });

  return validator;
});
